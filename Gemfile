source "https://rubygems.org"

# GitHub Pages compatible bundle (locks Jekyll & plugins to supported versions)
gem "github-pages", group: :jekyll_plugins

# Optional: If you need other plugins, add them carefully (must be supported by GitHub Pages)
# gem "jekyll-paginate"
